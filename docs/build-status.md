<!-- Generated by scripts/build_status_md.py from
     web/src/screens/Status.tsx. Do not edit by hand. -->

# Build status

Every claim on the submission deck against its true state in this
repository. Three values only. Nothing here is softened.

**48 built, 8 partial, 20 not built.**

## What the submitted deck claims, and what this repository measures

| Figure | Deck claims | This repository measures |
|---|---|---|
| Precision | `0.94` | `0.7901` |
| Recall | `0.87` | `0.3692` |
| F1 | `0.90` | `0.5033` |
| False merge rate | `0.012` | `0.0392` |
| Corpus size | `4,86,220 records` | `7,611 accused rows on the development corpus` |
| Resolved identities | `3,11,904` | `5,284` |
| Investigator questions | `150, 74% correct` | `not built` |

The deck figures were written at submission time, before Layers 3 to 8
existed, and were illustrative. Every figure in this repository is produced
by `make eval` and none is typed by hand. Where the two differ, this
repository is correct.

## Claim by claim

| Area | Claim | State | What is actually there |
|---|---|---|---|
| Engine | Layer 0, synthetic KSP corpus generator | **BUILT** | data/generator, pure standard library, seed 4471. 5,000 case default, verified linear to 150,000. |
| Engine | Layer 1, Indic normalisation and cross script folding | **BUILT** | engine/normalise/indic.py. Kannada to Latin transliteration written directly. 116 of 118 name pairs fold together across scripts. |
| Engine | Layer 2, blocking with phonetic and territorial keys | **BUILT** | engine/block. Reduction ratio 0.8897, pairs completeness 98.12%. |
| Engine | Layer 3, six signal feature extraction | **BUILT** | engine/features. All six computed and reported with coverage and AUC. Modelled as five, because lexical and phonetic are one channel, see ADR 017. |
| Engine | Layer 4, Fellegi Sunter with m and u by expectation maximisation | **PARTIAL** | engine/linkage. Fellegi Sunter and the frequency adjustment are built. EM does NOT fit m and u, it converges to the wrong mixture from every start. m is estimated from unsupervised leave one out seeds and EM fits only the mixing proportion. ADR 019. |
| Engine | Layer 4, inverse name frequency weighting | **BUILT** | Derived, not tuned. Worth +0.0131 F1 end to end. |
| Engine | Layer 5, constrained correlation clustering | **BUILT** | engine/cluster. Greedy agglomeration with cannot link enforced at merge time. Zero violations in the final partition. |
| Engine | Layer 6, collective iteration to a fixed point | **PARTIAL** | Runs, and does NOT converge. Two mechanisms were tried and both failed. Damping swept at 1.00, 0.50 and 0.30, none converged, and damping made the oscillation worse, ADR 021. Best partition selection on the engine own objective, measured across three corpus seeds, improved F1 on one and worsened it on two for a mean of -0.0000, ADR 025. Layer 6 is closed and ships the last iteration. |
| Engine | Layer 7, isotonic calibration and three way routing | **BUILT** | engine/calibrate. Routing at 0.92 and 0.65. False merge rate 0.0392 on the automatic band. |
| Engine | Layer 8, co offender graph and communities | **BUILT** | engine/downstream. 4,576 edges of which 1,401 recovered by resolution. Modularity 0.9973. |
| Engine | Layer 8, undetected case candidate ranking | **BUILT** | Measured against ground truth. Hit at 1 is 0.2067, at 10 is 0.5200, MRR 0.3115. |
| Engine | Layer 9, IPC to BNS reconciliation | **BUILT** | engine/reconcile. 18 correspondences covering every section the corpus contains. A naive query spanning July 2024 undercounts by 54.19%. |
| Evaluation | Precision, recall, F1 on a labelled gold set | **BUILT** | make eval. 0.7901, 0.3692, 0.5033 pairwise, the canonical headline. |
| Evaluation | Baselines: exact name, Soundex, Jaro Winkler, Indic phonetic | **BUILT** | All four measured. SUTRA is 7.2x exact name matching, F1 0.5033 against 0.0696. |
| Evaluation | Six signal ablation | **BUILT** | All six. Two have positive deltas, meaning removing them helps, and that is published rather than hidden. |
| Evaluation | Confusion matrix, convergence curve, blocking, latency | **BUILT** | All in eval/report.json and on the evaluation screen. |
| Evaluation | Public benchmark a third party can submit to | **BUILT** | benchmark/, IERB-P. Task README, one command generation, gold set with documented format, reference baseline, scorer and leaderboard. CC BY 4.0 data, MIT code. Every entry is currently ours, which is stated on the leaderboard as a weakness. |
| Evaluation | Precision recall curve and deployable operating points | **BUILT** | Forty thresholds, each a full re clustering. F1 optimal, F beta 0.5 optimal, precision 0.90 and 0.95 reported with recall and merge volume at each. F beta 0.5 named as the correct objective. |
| Evaluation | Sensitivity of the headline to the name vocabulary | **BUILT** | Swept at 86, 300, 1000 and 3000 forms. Precision moves 0.5770 to 0.8427 and the reduction ratio 0.8897 to 0.9731. The headline stays at the 86 form fixture and is labelled a floor. |
| Evaluation | 150 investigator questions with gold SQL | **BUILT** | eval/gold/questions.yaml. 150 questions across twelve investigative shapes, each with gold SQL against the KSP schema, 65 carrying a Kannada rendering. 76 of 150, 50.7%, cannot be answered on the raw schema at any level of interface sophistication. |
| Evaluation | 74 per cent accuracy on the question set | **NOT BUILT** | NOT measured and not claimed. Answering these from free text needs a natural language layer that does not exist and no language model runs anywhere in this system. The deck figure has no measurement behind it. What is measured is coverage: 39 answerable by the console today, 58 need a language layer, 53 impossible on the raw schema. |
| Engine | ComplainantDetails resolution | **BUILT** | F1 0.0047, precision 0.8000. Fixed by using the Address and PhoneNumber columns the table already carries and Accused does not. Transferring the prior from Accused and pooling all three tables were both tried first and both returned exactly 0.0000. See ADR 026. |
| Engine | Victim resolution | **NOT BUILT** | All three methods return exactly 0.0000. Victim has no address, no phone, no arresting officer and no co accused, and the only columns it carries beyond a name are the three that are excluded. Its oracle ceiling is 0.4839, so the features would carry it if the estimator could be bootstrapped, and there is nothing left to bootstrap from. See ADR 026. |
| Evaluation | A deployable operating point for automatic merging | **BUILT** | Precision 0.9508 at recall 0.1676, the highest recall that holds precision at or above 0.95. Reported beside the investigative point rather than buried in a table, because the operating point is a policy choice for the department and not a property of the method. |
| Engine | Layer 3g, recorded gender agreement | **BUILT** | A disagreement is strong evidence of a non match. Worth +0.0053 F0.5 at the 1.1% recording error rate the corpus models. The first version of this claim was measured on a corpus where gender could not be wrong and overstated the channel by about 74 per cent. Corrected in ADR 030. |
| Evaluation | Gender channel measured against a dirty field | **BUILT** | scripts/gender_noise_study.py sweeps the recording error rate from 0 to 10 per cent. The channel is positive up to 5 per cent and goes NEGATIVE at 10, which is an operating limit worth telling a department: if the gender field is wrong more than about one row in twelve, do not match on it. |
| Evaluation | The ceiling argument, stated | **BUILT** | With m and u fitted from ground truth this model form caps at F1 0.5937, so no linkage method can do much better on the fields this schema provides. SUTRA reaches 85% of it. The remaining gap is a data collection problem rather than a modelling one. ComplainantDetails has a phone number and a ceiling of 0.9781. Accused has neither and caps at 0.5937. |
| Engine | The person gap measured across all three tables | **BUILT** | engine/resolve_other.py. 13,299 person bearing rows across Accused, Victim and ComplainantDetails collapse to 8,268 actual people, and 5,651 same person relationships exist that no join on the raw schema can see. These counts come from ground truth, not from the resolver, so they stand whether or not it works. |
| Engine | Policy guard exercised on a table carrying protected columns | **BUILT** | Victim carries CasteID, ReligionID and OccupationID. The run asserts engine/policy.py rejects the raw header, projects the permitted columns, then asserts the projection passes. A control never seen to trip cannot be told apart from one that does not work. |
| Evaluation | make eval reproduces every figure from a clean clone | **BUILT** | make all runs generate, audit, block, resolve, downstream, eval, export. |
| Interface | Corpus audit screen | **BUILT** | Reads the real corpus reports. |
| Interface | Evaluation report screen | **BUILT** | Every figure read from eval.json, none hardcoded. |
| Interface | Identity review queue with evidence and routing | **BUILT** | Real per signal contributions. Merge and keep separate now record a decision, see the human in the loop row below. |
| Interface | Human in the loop review | **BUILT** | Qualified: client side and per browser. An operator accepts or refuses a merge, the row shows who decided it and when, and the decision appears on the audit trail. The log is append only, so a reversal adds an entry rather than removing one. Cleared by clearing site data. |
| Interface | Reversal of a decision, by the Reviewer only | **BUILT** | The Reviewer role gets a Reverse control on the audit trail. It appends a reversal naming the entry it reverses. No role can delete an entry, which is what makes the audit claim on this page true rather than aspirational. |
| Interface | Search by name variant or crime number | **BUILT** | In the masthead. Matches any recorded rendering of a name in either script, or a crime number, and jumps to the profile. Respects the active district scope, because it reads the already filtered reports rather than re deriving access. |
| Catalyst | Decision persistence to Data Store | **NOT BUILT** | Decisions live in localStorage in one browser. Two officers on two machines do not see each other work, and clearing site data clears the audit trail. A real deployment needs the log on a server, which needs Catalyst Data Store and authentication, and neither is built. |
| Interface | Cannot link conflict shown explicitly | **PARTIAL** | Built and prominent, but only one conflict exists in the exported review band, so it has little to show. |
| Interface | Offender profile with co offender network | **BUILT** | Cytoscape.js with a table equivalent behind a toggle. |
| Interface | Undetected case matcher screen | **BUILT** | With the measured hit rate beside the ranking. |
| Interface | Query console | **PARTIAL** | Structured parameterised queries with equivalent SQL. No natural language and no language model. |
| Interface | Trend queries correct across the BNS transition | **BUILT** | Layer 9 panel on the evaluation screen, naive against reconciled counts per offence. |
| Interface | Audit trail | **BUILT** | Rendered from runlog.json. |
| Engine | Layer 8, hotspots and trends | **BUILT** | engine/downstream/hotspots.py. Grid density, district aggregates, monthly trend with an anomaly flag fixed at 2.0 times the trailing 12 month median. |
| Interface | Hotspot and trend views | **BUILT** | The /hotspots screen. Inline SVG scatter, district bars and a trend line. Offender density computed on resolved identities, not case density. |
| Evaluation | Result holds at state scale, 150,000 cases | **PARTIAL** | Measured at 5,000, 10,000 and 20,000 cases and reported side by side. The full corpus was NOT run. Blocking proposes 3,04,88,08,835 candidate pairs at that size, which is a compute wall and is reported as one. |
| Interface | Space time cube | **NOT BUILT** | Not started. The hotspots screen covers the spatial and temporal views without it. |
| Interface | Resolution collapse animation | **NOT BUILT** | Not started. |
| Interface | Kannada interface | **PARTIAL** | Interface chrome only. Navigation, panel titles, column headers, status words, buttons and the audit strip. Explanatory prose stays in English and no engine output is translated. |
| Interface | Kannada natural language querying | **NOT BUILT** | Not started, and it needs a language model, which this system does not have anywhere. |
| Interface | Kannada speech input | **NOT BUILT** | Not started. Zia speech is not wired. |
| Engine | Kannada handled by the engine | **BUILT** | Layer 1 only, and that is the part that matters. Kannada names are transliterated and folded so a name written in either script reaches the same key. Nothing about the interface language touches resolution. |
| Interface | Role and jurisdiction scoping | **PARTIAL** | Built as a client side view filter. Four roles, one of them district scoped. The selection actually filters cases, identities, network edges and the review queue, and every count is recomputed from the filtered rows. It demonstrates the access model. It is NOT enforcement. |
| Catalyst | Server side enforcement of role scoping | **NOT BUILT** | The role is chosen from a dropdown in the browser and the underlying JSON is served in full to anyone who requests it. Opening the network tab shows every district regardless of the role selected. Real enforcement means the server decides what to send, after Catalyst Authentication has established who is asking. |
| Interface | Print stylesheet | **BUILT** | web/src/styles/print.css, written for paper. |
| Interface | Accessibility, 4.5 to 1 contrast verified in code | **BUILT** | web/scripts/check-contrast.mjs fails the build below 4.5. All 32 text pairs pass. |
| Interface | Responsive to 380px | **PARTIAL** | Layout collapses at breakpoints but has not been tested on a real device. |
| Stack | Python 3, NumPy, SciPy, scikit-learn | **BUILT** | scikit-learn used for TF-IDF and isotonic regression only. |
| Stack | NetworkX | **BUILT** | Clustering repair, co offender graph, community detection. |
| Stack | React, TypeScript, Vite | **BUILT** | web/, strict mode with noUncheckedIndexedAccess. |
| Stack | Cytoscape.js | **BUILT** | The co offender network on the profile screen. |
| Stack | Splink | **NOT BUILT** | Not used. Fellegi Sunter is implemented directly in engine/linkage. |
| Stack | RapidFuzz | **NOT BUILT** | Not used. Jaro Winkler and token set ratio are written out in engine/features/signals.py. |
| Stack | jellyfish | **NOT BUILT** | Installed but not imported. Phonetic folding is our own Indic scheme, see ADR 003. |
| Stack | Polars | **NOT BUILT** | Not used. csv and NumPy throughout. |
| Stack | leidenalg or igraph | **NOT BUILT** | Not installed. Communities use NetworkX greedy modularity instead, with connectivity verified explicitly. ADR 008 amended. |
| Stack | sentence-transformers or an embedding model | **NOT BUILT** | Not used. Modus operandi similarity is TF-IDF cosine over word and character n grams. |
| Stack | three.js or WebGL 3D | **NOT BUILT** | Not used. The network is 2D, because a co offender neighbourhood is not dimensional data. |
| Stack | Vendored fonts, no external hosts | **BUILT** | Four families as local woff2, verified by verify-dist. |
| Catalyst | Web Client Hosting | **BUILT** | The deployed surface. Static bundle, index.html at the archive root. |
| Catalyst | QuickML for LLM and embeddings | **NOT BUILT** | No LLM anywhere in the system. Nothing to host. |
| Catalyst | Zia for Kannada speech | **NOT BUILT** | No speech input. |
| Catalyst | Data Store or Stratus | **NOT BUILT** | Not used. The deployed surface serves precomputed JSON, per ADR 002. |
| Catalyst | Catalyst Authentication | **NOT BUILT** | No authentication. The surface is read only and carries synthetic data. |
| Catalyst | Job Scheduling for nightly resolution | **NOT BUILT** | Resolution runs locally by hand. The batch architecture is built, the scheduler binding is not. |
| Catalyst | No third party hosted API is called | **BUILT** | Verified at build time. verify-dist rejects any bundle that would issue a cross origin request. |

## On the libraries we did not use

The linkage model, the Indic phonetic folding and the string metrics are
implemented directly rather than imported from Splink, jellyfish or
RapidFuzz. That was deliberate and it buys one specific thing. Every
weight in a merge score traces to a fitted m and u, can be shown to an
investigator as a list of contributions that sums to the total, and can
be argued with. A library that returns a score cannot do that, and a merge
an officer cannot interrogate is a merge they should not act on. The same
reasoning rules out an embedding model for modus operandi similarity,
where TF-IDF over word and character n grams is inspectable and a sentence
transformer is not.

## On the deployed surface being static

Resolution is a nightly batch job by design, recorded as ADR 002.
Expectation maximisation over every comparison vector, iteration to a
fixed point and corpus wide frequency tables are global computations that
cannot run inside a request. So the engine runs locally, exports JSON, and
Catalyst serves it. That is why most Catalyst services show as not built.
There is no function, no database and no runtime dependency, because the
architecture does not need one. The honest cost is staleness, and the
provenance bar carries the resolution timestamp on every screen so a
reader always knows how old the answer is.
